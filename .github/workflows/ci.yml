name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10"]

    steps:
    - uses: actions/checkout@v3

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}

    - name: Set up Conda
      uses: conda-incubator/setup-miniconda@v2
      with:
        activate-environment: test-env
        environment-file: https://raw.githubusercontent.com/qiime2/distributions/refs/heads/dev/2025.4/amplicon/released/qiime2-amplicon-ubuntu-latest-conda.yml
        auto-activate-base: false

    - name: Install dependencies
      shell: bash -l {0}
      run: |
        conda config --set always_yes yes
        conda config --add channels bioconda
        conda config --add channels conda-forge
        conda install -q pytest pytest-cov
        conda install -c conda-forge blas=1.1
        pip install -q flake8 scnic
        pip install -q https://github.com/qiime2/q2lint/archive/master.zip
        make install

    - name: Run linting
      shell: bash -l {0}
      run: |
        make lint

    - name: Test with pytest
      shell: bash -l {0}
      run: |
        python -m pytest tests/